# قائمة المهام التفصيلية (Tasks Breakdown Checklist)

خطة تنفيذية مفككة لكافة مراحل المشروع بصيغة مهام قابلة للتتبع والتنفيذ (Checklist)، مقسمة حسب الطبقات الفنية: قاعدة البيانات، منطق الباك إند، واجهات Livewire و Blade، والاختبارات ومراقبة الجودة.

---

## المرحلة الأولى: النواة الأساسية للنظام (Phase 1: Core MVP)

### 1.1 قاعدة البيانات والنماذج (Database & Models)
- [x] إعداد مشروع Laravel وتثبيت الحزم الأساسية (Livewire 4, Spatie Permission).
- [x] إنشاء Migration جدول الأصناف `items` مع التأكيد الصارم على حقول `DECIMAL(12,3)` وتصنيف الأصناف `category`.
- [x] إنشاء Migration جدول العملاء `customers` مع حقل `current_balance DECIMAL(12,3)`.
- [x] إنشاء Migration جدول الموردين `suppliers` مع حقل `current_balance DECIMAL(12,3)`.
- [x] إنشاء Migration جدول فواتير المشتريات `purchases` وجدول بنودها `purchase_items`.
- [x] إنشاء Migration جدول فواتير المبيعات `invoices` وجدول بنودها `invoice_items` مع الفهارس والقيود الأجنبية.
- [x] إنشاء Migration جدول حركات المخزون الموحد `stock_movements`.
- [x] إنشاء Migration جدول إيداعات المخزون والتسويات `stock_deposits`.
- [x] إنشاء نماذج Eloquent (`Item`, `Customer`, `Supplier`, `Purchase`, `PurchaseItem`, `Invoice`, `InvoiceItem`, `StockMovement`, `StockDeposit`) وضبط العلاقات و Casts الرقمية.

### 1.2 منطق الأعمال والخدمات الخلفية (Backend Services)
- [x] بناء خدمة `StockService` للتحقق من الأرصدة، الصرف `stock_out`، التوريد `stock_in`، وتسجيل `StockMovement`.
- [x] بناء خدمة `PurchaseService` لإدارة فواتير الشراء، زيادة المخزون، وتحديث متوسط التكلفة وحسابات الموردين.
- [x] بناء خدمة `InvoiceService` لتنفيذ المعاملة المالية `DB::transaction()` وتطبيق القفل السطري `lockForUpdate()` على الأصناف.
- [x] برمجة دوال احتساب الخصومات المزدوجة (خصم السطر + خصم الفاتورة) واحتساب الصافي بدقة باستخدام `bcmath`.
- [x] منع البيع في حالة عدم كفاية المخزون ورمي استثناء مخصص وتراجع المعاملة (Rollback).

### 1.3 الواجهات ومكونات Livewire (Frontend & Livewire)
- [x] بناء قالب التخطيط العام `layouts/app.blade.php` متوافق مع Tailwind CSS و Flowbite و RTL.
- [x] بناء مكون Livewire لإدارة ودليل الأصناف `ItemList` مع البحث الفوري وحالات التفعيل والتعطيل.
- [x] بناء مكون نافذة سريعة لإضافة صنف `ItemQuickCreateModal`.
- [x] بناء مكون Livewire لإدارة العملاء `CustomerList` والموردين `SupplierList`.
- [x] بناء شاشة فواتير الشراء التفاعلية `PurchaseCreate` مع أزرار الأوزان الكبيرة وإظهار الكميات الموردة بوضوح.
- [x] بناء شاشة فواتير البيع الحية `InvoiceCreate` مع أزرار أوزان البن والشاي (ثمن، ربع، نصف، كيلو) والبحث السريع.

### 1.4 الاختبارات والجودة (Testing & QA)
- [x] كتابة اختبارات Unit Tests لحسابات الخصومات وصافي الفاتورة.
- [x] كتابة اختبارات Feature Tests لمعاملة اعتماد الفاتورة وتأكيد خصم المخزون وإنشاء سجل `StockMovement`.
- [x] اختبار القفل السطري `lockForUpdate()` ومحاكاة محاولة بيع نفس الرصيد المحدود من جلستين متزامنتين.

---

## المرحلة الثانية: المدفوعات والحسابات والمرتجعات (Phase 2: Payments, Ledgers & Returns)

### 2.1 قاعدة البيانات والنماذج (Database & Models)
- [x] إنشاء Migration جدول المدفوعات وسندات القبض والصرف `payments`.
- [x] إنشاء Migration جدول المرتجعات `returns` وجدول بنود المرتجعات `return_items`.
- [x] إنشاء نماذج Eloquent (`Payment`, `ReturnDocument`, `ReturnItem`) وضبط العلاقات مع الفواتير والعملاء والموردين.

### 2.2 منطق الأعمال والخدمات الخلفية (Backend Services)
- [x] بناء خدمة `PaymentService` لتسجيل الدفعات النقدية والمجزأة وتحديث حالة سداد الفاتورة (`paid`, `partially_paid`, `unpaid`).
- [x] بناء خدمة `CustomerBalanceService` لتحديث الرصيد التراكمي للعميل واستخراج بيانات كشف الحساب الزمني.
- [x] بناء خدمة `ReturnService` لمعالجة المرتجعات الجزئية وإعادة الكمية للمخزون وعكس الأثر المالي على العميل والمورد.
- [x] برمجة منطق إلغاء الفاتورة بالكامل (Cancellation) وعكس حركات المخزون والقيود المالية وتغيير الحالة إلى `cancelled`.

### 2.3 الواجهات ومكونات Livewire (Frontend & Livewire)
- [x] بناء مكون نافذة سند القبض `PaymentModal` لتسجيل دفعات العملاء وسداد الفواتير الآجلة.
- [x] بناء شاشة كشف حساب العميل التفاعلية `CustomerStatement` مع الفلترة بالتواريخ.
- [x] بناء شاشة كشف حساب المورد `SupplierStatement`.
- [x] بناء نافذة ومكون المرتجعات `ReturnCreate` و `ReturnIndex` لاختيار الأصناف وإدخال الكميات المرتجعة وسبب المرتجع.
- [x] إضافة خيار "إلغاء الفاتورة" للمستخدمين المصرح لهم مع تأكيد مسبب.

### 2.4 الاختبارات والجودة (Testing & QA)
- [x] اختبار عمليات السداد المتعدد لنفس الفاتورة والتحقق من حساب المتبقي وحالة السداد.
- [x] اختبار المرتجع الجزئي والتأكد من إضافة الكمية المرتجعة فقط إلى المخزن بدقة.
- [x] اختبار إلغاء الفاتورة والتأكد من عودة كامل كميات البنود للمخزن وتصفير رصيد الفاتورة دون حذفها.

---

## المرحلة الثالثة: التكاليف، الأرباح، ولوحة التحكم (Phase 3: Costing, Profits & Dashboard)

### 3.1 قاعدة البيانات والخدمات (Backend Logic & Profit Algorithms)
- [x] بناء خدمة `ProfitService` لحساب تكلفة البضاعة المباعة (COGS) بناءً على التكلفة المثبتة في أسطر الفاتورة.
- [x] تطبيق معادلات احتساب صافي الأرباح وهامش الربح للفاتورة، وللصنف، وللفترة الزمنية.
- [x] تفعيل وتحديث متوسط التكلفة المرجح (Weighted Average Cost) في كل عملية شراء جديدة.
- [x] بناء استعلامات إحصائيات لوحة التحكم المجمعة وسرعة الاستجابة.

### 3.2 الواجهات ومكونات Livewire (Frontend & Livewire)
- [x] بناء مكون إحصائيات لوحة التحكم `Dashboard` لعرض مبيعات اليوم، التحصيل، الديون المتأخرة، والأرباح.
- [x] بناء ويدجت تنبيهات النواقص `lowStockItems` لعرض الأصناف التي بلغت الحد الأدنى.
- [x] بناء شاشة التقارير والأرباح الشاملة `ReportsIndex` لتحليل ربحية كل صنف وقيمة بضاعة المخزن بالتكلفة وسعر البيع.

---

## المرحلة الرابعة: الطباعة، التصدير، وتطبيق الويب (Phase 4: Printing, Export & PWA)

### 4.1 قوالب الطباعة و CSS (Print Architecture)
- [x] تصميم وهيكلة قالب الطباعة القياسي `layouts/print-a4.blade.php` وضبط هوامش `@page` ودعم RTL.
- [x] تصميم وهيكلة قالب الطباعة الحراري `layouts/print-thermal.blade.php` المخصص لرول 80mm وإيصالات الكاشير.
- [x] إخفاء أشرطة القوائم والسايدبار والأزرار في ملفات الـ Print CSS واستدعاء `window.print()` بسلاسة.
- [x] بناء شاشات طباعة الفاتورة، سند القبض، وكشف الحساب لكلا القياسين.

### 4.2 التصدير وتطبيق الويب التفاعلي (Export & PWA)
- [x] بناء خدمة `ExportService` لتصدير كشف حساب العميل، كشف حساب المورد، وجرد المخزون لملفات Excel/CSV مع تضمين ترميز UTF-8 BOM لدعم اللغة العربية في إكسيل.
- [x] بناء ملف `manifest.json` و `sw.js` وربطهما لتحويل النظام إلى تطبيق ويب تقدمي (PWA) قابل للتثبيت على هواتف وأجهزة الكاشير المحمولة.

---

## المرحلة الخامسة: نظام الفروع وعربات التوزيع المتنقلة (Multi-Store & Wholesale Van Distribution)

### 5.1 البنية التحتية وقاعدة البيانات (Phase 1: Database & Migrations)
- [x] إنشاء جدول الفروع وعربات التوزيع والمخازن `stores`.
- [x] إنشاء جدول جرد وأسعار الفروع `store_stocks` مع دعم `custom_selling_price`.
- [x] إنشاء جدول أذونات تحويل البضاعة `stock_transfers` وبنودها `stock_transfer_items`.
- [x] إضافة `store_id` لكافة الجداول التشغيلية وترحيل السجلات القديمة تلقائياً إلى الفرع الرئيسي عبر `StoreSystemMigrationSeeder`.

### 5.2 طبقة الخدمات ومنطق التسعير الذكي (Phase 2: Services & Logic)
- [x] بناء خدمة التحويلات وشحن البضاعة `StockTransferService` مع القفل السطري `lockForUpdate()`.
- [x] تحديث `StockService` و `InvoiceService` و `PurchaseService` و `ReturnService` لتعمل على مستوى الفرع.
- [x] بناء خدمة ذاكرة الأسعار `CustomerPricingHelper` لاسترجاع آخر سعر بيع للعميل للصنف.
- [x] بناء وتفعيل `StoreScope` و `StoreAccess` لحماية صلاحيات الفروع والتبديل السريع.

### 5.3 الواجهات ونقطة البيع التفاعلية (Phase 3: UI & POS Experience)
- [x] بناء محوّل الفروع `StoreSwitcher` في الهيدر العلوي.
- [x] تطوير نقطة البيع `InvoiceCreate` لإظهار رصيد الفرع وشارة تذكّر آخر سعر للعميل بضغطة زر `[⚡ تطبيق السعر]`.
- [x] بناء شاشات إدارة الفروع `/stores` وتعيين المناديب.
- [x] بناء شاشة جرد وتسعير الفروع `/store-stocks`.
- [x] بناء شاشات أذونات التحويل والشحن `/stock-transfers` و `/stock-transfers/create`.

### 5.4 الورديات والتقارير المجمعة (Phase 4: Shifts & Consolidated Reports)
- [x] عزل ورديات الخزينة والصندوق لكل فرع / عربية توزيع في `ShiftService`.
- [x] فلترة اليومية وحركة الدرج بالفرع وتسوية عهدة العربية اليومية في `DailyJournalIndex`.
- [x] فلترة تقارير الأرباح وتكلفة المبيعات بالفرع وإضافة جدول مقارنة أداء ومبيعات الفروع وعربيات التوزيع في `ReportsIndex`.
- [x] حل مشكلة إضافة العميل والرصيد الافتتاحي وتأكيد خلو النظام من أي أخطاء 500.

---

## المرحلة الثامنة: السايدبار المصغر وشاشة نقطة البيع للمس (Phase 8: Collapsible Mini-Sidebar & Touch POS)
- [x] تحويل القائمة الجانبية (Sidebar) إلى قائمة ذكية قابلة للتصغير (`w-20` إلى `w-72`) مع زر تحكم وحفظ التفضيل في المتصفح.
- [x] بناء نوافذ عائمة (Hover Tooltips / Flyouts) أنيقة باللغة العربية تظهر عند التمرير بالماوس في الوضع المصغر.
- [x] تفعيل التصغير التلقائي والإجباري للسايدبار في صفحة نقطة البيع (`/invoices/create`) لتوفير أقصى مساحة للكاشير.
- [x] إعادة تصميم وهندسة شاشة المبيعات (`InvoiceCreate`) لتكون مهيأة بالكامل للمس بالأصابع (أهداف لمس واسعة `48px-56px`).
- [x] إضافة بطاقات أصناف مكبرة للمس السريع مع أزرار أوزان سريعة (`1/8`, `1/4`, `1/2`, `1كجم`) وأزرار كميات `[+]` و `[-]` في السلة.
- [x] إضافة لوحة أرقام لمس تفاعلية (Touch Keypad) وأزرار سداد نقدي سريع (`المبلغ بالضبط`, `50`, `100`, `200`).
- [x] إيقاف رابط خلاط التوليفات مؤقتاً بالتعليق دون حذفه.
- [x] اجتياز 100/100 اختبار PHPUnit واختبارات Playwright الحية على السيرفر بنجاح 100%.

